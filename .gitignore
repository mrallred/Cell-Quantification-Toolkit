# Jython bytecode
*py.class
__pycache__/

# macOS
.DS_Store

# ilastik classifiers: too large to ship in the repo or on the update site
# (see docs/creating_workflows.md)
models/*

# Runtime marker recording which built-in workflows have been seeded
workflow_defs/.seeded
