# Moved to lib/pipeline_runner.py so workflow_config can import it as a sibling
# module (reliable relative import). This stub remains only because the sandbox
# cannot delete files; it intentionally defines nothing.
